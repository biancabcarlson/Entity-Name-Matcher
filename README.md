# Entity-Name-Matcher
Given a list of names, flags pairs that are likely the same entity based on standard fuzzy string matching (Levenshtein distance) — nicknames, typos, formatting differences, missing middle names. Outputs a review list only; it draws no conclusions and applies no fraud logic. Pure text-similarity utility, same algorithm used in spell-checkers.
